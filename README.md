# BEE208-LED-Resistor-Value-Calculator-System  


**PROJECT TITLE: LED RESISTOR VALUE CALCULATOR SYSTEM**  


| NAMES | INDEX NUMBERS |
| :--- | :--- |
| AIDOO FREDERICK ANSON | 01243029B |
| ISSAHAKU ABDULLAH | 01244271B |
| TORGBO COMFORT | 01241532B |
| DIVINE LOVE SOWAH | 01243141B |
| AWINDINABA JUSTICE | 01244230B |
| KYAMASI OWUSU AGYEMANG | 01240187B |
| IVAN ASAFO ADJEI | 01243590B |
| SEDZRO KELVIN | 01243999B |
| ANTWI JULIOS | 01242969B |
| STEPHEN EHWEI | 01242650B |


**PROBLEM STATEMENT**  

Students and beginners in electronics often choose LED resistors by guessing or copying values from other circuits. This can cause the LED to draw too much current or appear too dim. A simple software tool is needed to calculate the correct resistor value and provide basic safety feedback before the circuit is built.  

**AIM OF THE PROJECT**  

Students and beginners in electronics often choose LED resistors by guessing or copying values from other circuits. This can cause the LED to draw too much current or appear too dim. A simple software tool is needed to calculate the correct resistor value and provide basic safety feedback before the circuit is built.  

**OBJECTIVES OF THE PROJECT**  

.Accept user inputs for LED label/colour, circuit supply voltage, LED forward voltage, and desired current in milliamperes  

.Enforce strict input validation rules to reject negative electrical parameters, zero current, and physically impossible circuit loops where the supply voltage is less than or equal to the forward voltage  

.Apply Ohm's Law and Joule's Law sequentially to calculate resistance and power values.  

.Classify configurations into distinct statuses (Design Accepted, Warning, or Invalid Design).  

.Output results to the console window and compile them into `led_resistor_report.txt`.  


**EXPLANATION OF THE LED RESISTOR CALCULATION FORMULA**  

The application applies standard electrical engineering laws sequentially using the following text-based steps:  


1. **Current Conversion:**
   
The target LED current value is initially received from the user in milliamperes. To prepare this value for standard electrical equations, the system converts it into Amperes by taking the desired LED current in milliamperes and dividing it by one thousand.  


3. **Ohm's Law for Calculating Resistance:**
   
To determine the exact size of the current-limiting resistor required, the system calculates the total voltage drop that the resistor needs to handle. This is found by taking the circuit supply voltage and subtracting the LED forward voltage. The resulting voltage difference is then divided by the previously converted LED current in Amperes to give the recommended resistor value in ohms.  


5. **Joule's Law for Power Dissipation:**
   
To ensure the physical resistor can safely handle the heat generated by the circuit without burning out, the minimum resistor power rating in watts is calculated. This is found by taking the LED current in Amperes, multiplying it by itself to square it, and then multiplying that result by the calculated recommended resistor value in ohms.

**ALGORITHM AND PSEUDOCODE SUMMARY**  


**ALGORITHM SUMMARY**  

The program initializes an outer execution loop based on the total number of circuits specified by the user. For each loop iteration, the system prompts for input data and routes it through an input validation decision block. If the electrical values are mathematically or physically invalid, the program logs an "Invalid Design" status string and skips all calculation blocks to jump directly to the loop condition evaluation. If the inputs are valid, the program processes the conversion, resistance, and power formulas in sequential order, checks the current against a thirty-milliamp safety threshold to append the correct status message, and prints the result to the console window. Once all specified circuits have completed their turn through the loop, the system executes file-writing streams to save a complete evaluation summary to an external plain text file.  
 

**PSEUDOCODE SUMMARY**  

IF (supplyVoltage <= forwardVoltage) OR (ledCurrentMilliAmps <= 0) THEN  

    SET designStatus = "Invalid Design"  
    
ELSE  

    CALCULATE ledCurrentAmps = ledCurrentMilliAmps / 1000  
    
    CALCULATE resistorValue = (supplyVoltage - forwardVoltage) / ledCurrentAmps  
    
    CALCULATE resistorPower = ledCurrentAmps * ledCurrentAmps * resistorValue  
    
    
    IF (ledCurrentMilliAmps > 30) THEN  
    
        SET designStatus = "Warning: LED current may be too high for a standard LED."  
        
    ELSE  
    
        SET designStatus = "Design Accepted"  
        
    END IF  
    
    
    DISPLAY resistorValue, resistorPower, designStatus  
    
END IF  

**FLOWCHART IMAGE**  

[FLOW CHART.pdf](https://github.com/user-attachments/files/29927057/FLOW.CHART.pdf)


**EXPLANATION OF THE C++ CONCEPT USED**  
Instead of having data scattered everywhere, we used a LEDCircuit class. Think of a class like a blank blueprint for a circuit card, and an object as the actual card you build from it. It keeps all the information for a single circuit—like its name, voltages, and results—neatly locked together in one safe box.  

**VECTORS (A DYNAMIC SMART LIST)**  

Standard code lists usually require you to know the exact number of items ahead of time, and they cannot change size later. We used a C++ vector, which is like a smart, stretchy digital list. It automatically grows bigger every time the user adds another LED circuit, keeping an accurate history of the entries.  

**IF-ELSE STATEMENT**  

These are the decision junctions in our code. They act like traffic police officers checking the numbers. If a user types in a bad number, the if statement catches it and flags an error. If the numbers look good, the else path lets the math calculation run normally.  

**SCREENSHOTS OF THE PROGRAMME OUTPUT**  

This proves your Ohm's Law and power formulas are working perfectly.  

<img width="672" height="472" alt="Screenshot 2026-07-10 113216" src="https://github.com/user-attachments/assets/cb3717e6-d709-4f84-943e-227ffb8728d6" />  

This proves your safety classification rules work

<img width="932" height="330" alt="Screenshot 2026-07-10 113432" src="https://github.com/user-attachments/assets/f96d2602-395c-4383-9246-48994d885bbc" />  

This proves your system actively rejects wrong electrical inputs and prevents system crashes.

<img width="1137" height="81" alt="Screenshot 2026-07-10 113617" src="https://github.com/user-attachments/assets/590ef247-4723-4430-b4f5-1b195c3ab615" />  

**CHALLENGES FACED AND SOLUTION**  

**FIRST PROBLEM: HANDLING MISTAKES AND BAD INPUT**  

**What went wrong:** At first, if a user typed in impossible numbers (like a supply voltage that was too low for the LED), the program got confused. It would either get stuck spinning in an endless loop forever, or it would completely skip the rest of the student's circuits.  


**How we fixed it:** We changed our flowchart and code logic. Now, if someone types a wrong number, the program immediately labels it an "Invalid Design" and moves straight to the loop counter. This lets the program keep running smoothly so the user can calculate their remaining circuits without the app crashing.  

**CONCLUSION**  

This project successfully builds an easy-to-use software tool for calculating LED resistors using C++.

Instead of guessing resistor values—which can easily burn out real electronics or make components too dim—our program uses standard electrical laws to find the exact parts needed in seconds.

By adding smart features like input validation (to catch user mistakes before they cause problems), a safety warning system for high currents, and automatic text file saving, our team created a reliable system. It perfectly bridges the gap between electrical engineering theories and practical computer programming, giving students a safe way to plan out their hardware designs.














